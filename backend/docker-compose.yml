services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: camel
      POSTGRES_PASSWORD: camel
      POSTGRES_DB: camel
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U camel"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  db_data:
